<?php

declare(strict_types=1);

namespace App\Actions;

abstract class BaseAction
{
    // A base class for actions to share common utilities if needed.
    // Each Action class should define its own execute() or handle() method
    // with its specific type hints.
}
